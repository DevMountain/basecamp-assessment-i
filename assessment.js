/**
 * #1
 * 
 * Create a variable called 'myName' and assign it to your name as a string
 */

// CODE HERE

/**
 * #2
 * 
 * Create a variable called 'faveNum' and assign it to your favorite number
 */

// CODE HERE

/**
 * #3
 * 
 * Create a variable called 'lovesCode' and assign it to the boolean value true
 */

// CODE HERE

/**
 * #4
 * 
 * Create a new variable called 'me'. 
 * Assign it a string that uses the 'myName' and 'faveNum' variables. 
 * Example: My name is Bryan and my favorite number is 3.
 */

// CODE HERE

/**
 * #5
 * 
 * Create a new variable called 'trueFaveNum'.
 * 'trueFaveNum' should equal true when comparing the value of the 'faveNum' variable.
 * HINT: Use a comparison operator
 */

// CODE HERE

/**
 * #6
 * 
 * Create a new variable called 'notHakeem'. 
 * Assign this variable the result of a comparison 
 * between the 'myName' variable and the string "Hakeem Olajuwon"
 */

// CODE HERE

/**
 * #7
 * 
 * Create an if statement that checks the 'age' variable below.
 * If age is greater than 16, assign 'canDrive' to true otherwise assign 'canDrive' to false.
 * The 'canDrive' variable will be assigned by the unit test so do not adjust it.
 */

var age = 18;
var canDrive;

// CODE HERE

/**
 * #8
 * 
 * Create an if statement inside of the 'responseCreator' function. 
 * Assign the 'colorResponse' variable the value based off of a passed in parameter. 
 * The parameter value passed in will be added in the unit test.
 * If the parameter value is "green" assign the 'colorResponse' variable to "Green is okay".
 * If the parameter value is "red" assign the 'colorResponse' variable to "I heart red".
 * Otherwise assign the 'colorResponse' variable to the string "What is your favorite color?".
 * Return the 'colorResponse' variable at the end of the function.
 */

var colorResponse;

var responseCreator = function (parameter) {
  // CODE HERE
}

/**
 * #9
 * 
 * Create a function called 'sum' that takes in one parameter.
 * Return the sum of that parameter added to itself.
 * HINT: Use an Assignment Operator
 */ 

// CODE HERE

/**
 * #10
 * 
 * Create a function called 'greeter' that takes in two parameters, name and number.
 * Have 'greeter' return a string that says something like this:
 * "Hi! I am Bryan and I am 28 years old" 
 */  

// CODE HERE
